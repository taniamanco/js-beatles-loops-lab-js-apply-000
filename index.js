// add solution here
function theBeatlesPlay(musiciansArr, instrumentsArr) {
  let musicianPlay = [];
  for (let i = musiciansArr[0]; i < musiciansArr.length; i++) {
    musician.push(musiciansArr[i]);
  } for (let j = instrumentsArr[0]; j < instrumentsArr.length; j++) {
      instrument.push(instrumentsArr[j]);
    }
}
